# Committed on purpose, like client/.env — VITE_ vars are public in the
# built bundle anyway, so there's no secret here to protect.
# Replace with your actual Hugging Face Space URL after first deploy.
VITE_API_URL=https://your-space-name.hf.space/api
